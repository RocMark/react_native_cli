import { Dimensions } from 'react-native'

const colors = {
  primary: '',
}

const containerSize = {
  fullHeight: Dimensions.get('window').height,
  fullWidth: Dimensions.get('window').width,
}

const fontSize = {
  sm: 12,
  md: 18,
  lg: 28,
}

export { colors, containerSize, fontSize }
